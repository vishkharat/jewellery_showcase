<div class="row flex-lg-row-reverse align-items-center g-5 py-4 mb-4">
    <div class="col-12 col-lg-6">
      <img src="/img/hero-gold.png" width="607" height="510" class="d-block mx-lg-auto img-fluid" loading="lazy" alt="Cooking With Node.js">
    </div>
  
    <div class="col-12 col-lg-6">
      <h1 class="display-5 fw-bold mb-3">Huge Collection of jewelry </h1>
      <p class="lead">
        Adorn yourself in timeless elegance with our exquisite jewelry collection. Discover the perfect blend of sophistication and style.
      </p>
  
      <div class="d-grid gap-2 d-md-flex justify-content-md-start">
        <a href="/explore-latest" class="btn btn-primary btn-dark btn-lg px-4 me-md-2">Explore Latest</a>
        <a href="/explore-random" class="btn btn-outline-secondary btn-lg px-4 me-md-2">Show Random</a>
      </div>
  
    </div>
  
  </div>

  <!--category start-->
  <div class="row row-cols-2 row-cols-lg-6 g-2 g-lg-3 py-4">

    <% if(categories != '') { %>
        <% categories.forEach(function(category, index) { %>
          <a href="/categories/<%- category.name %>" class="col text-center category__link">
            <div class="category__img shadow">
              <img src="img/<%- category.image %>" alt="<%- category.name %>" loading="lazy">
            </div>
            <div class="pt-1"><%- category.name %></div>
          </a>
        <% }) %>
      <% } %>
   

    <a href="/categories" class="col text-center category__link">
        <div class = "category__img shadow">
            <img src="img/view-all.png" alt="view all category" loading="lazy">
        </div>
        <div class = "pt-1">view all</div>
    </a>
</div>
    <!--category end-->

    <!--latest start-->
    <section class="pb-4 pt-4">
        <div class="d-flex mb-2 align-items-center">
          <h2>Latest Designs</h2>
          <a href="/explore-latest" class="ms-auto">View More</a>
        </div>
      
        <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">

          <% if(gold != '') { %>
            <% gold.latest.forEach(function(design, index){ %>
            <a href="/design/<%= design._id %>" class="col text-center category__link">
              <div class="category__img category__img--large shadow">
                <img src="/uploads/<%= design .image %>" alt="<%= design.name %>" loading="lazy">
              </div>
              <div class="pt-1"><%= design.name %></div>
            </a>
            <% }) %>
          <% } else { %>
            <p>No items found.</p>
          <% } %>

           
        </div>
    </section>
<!--latest end-->

  <!--Mnagalsutra start-->
  <section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
      <h2>Mangalsutra Designs</h2>
      <a href="/categories/Mangalsutra" class="ms-auto">View More</a>
    </div>
  
    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">

      <% if(typeof gold.mangalsutra !== 'undefined' && gold.mangalsutra.length > 0) { %>
        <% gold.mangalsutra.forEach(function(design, index){ %>
        <a href="/design/<%= design._id %>" class="col text-center category__link">
          <div class="category__img category__img--large shadow">
            <img src="/uploads/<%= design .image %>" alt="<%= design.name %>" loading="lazy">
          </div>
          <div class="pt-1"><%= design.name %></div>
        </a>
        <% }) %>
      <% } else { %>
        <p>No items found.</p>
      <% } %>

       
    </div>
</section>
<!--Mangalsutra end-->

 <!--Couple Rings start-->
 <section class="pb-4 pt-4">
  <div class="d-flex mb-2 align-items-center">
    <h2>Couple-Rings Designs</h2>
    <a href="/categories/Couple Rings" class="ms-auto">View More</a>
  </div>

  <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">

    <% if(typeof gold.couplerings !== 'undefined' && gold.couplerings.length > 0) { %>
      <% gold.couplerings.forEach(function(design, index){ %>
      <a href="/design/<%= design._id %>" class="col text-center category__link">
        <div class="category__img category__img--large shadow">
          <img src="/uploads/<%= design .image %>" alt="<%= design.name %>" loading="lazy">
        </div>
        <div class="pt-1"><%= design.name %></div>
      </a>
      <% }) %>
    <% } else { %>
      <p>No items found.</p>
    <% } %>

     
  </div>
</section>
<!--Couple rings end-->


<!--SilverPayal start-->
<section class="pb-4 pt-4">
  <!-- Necklaces Category -->
  <div class="d-flex mb-2 align-items-center">
    <h2>Silver-Bracelets Designs</h2>
    <a href="/categories/Silver Bracelets" class="ms-auto">View More</a>
  </div>
  <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
    <% if(typeof gold.bracelets !== 'undefined' && gold.bracelets .length > 0) { %>
      <% gold.bracelets .forEach(function(design, index){ %>
      <a href="/design/<%= design._id %>" class="col text-center category__link">
        <div class="category__img category__img--large shadow">
          <img src="/uploads/<%= design.image %>" alt="<%= design.name %>" loading="lazy">
        </div>
        <div class="pt-1"><%= design.name %></div>
      </a>
      <% }) %>
    <% } else { %>
      <p>No items found.</p>
    <% }%>
  </div>
</section>

<!--SilverPayal end -->

 































<!--submit start-->



        <section class = "px-4 py-5 my-5 text-center">
            <img src="/img/submission1.png" class = "d-block mx-auto mb-4 img-fluid" alt="Add Your New Design Today" width="566" height="208" loading="lazy">
            <h1 class="display-5 fw-bold">Add Your New Design Today</h1>
            <div class="col-lg-6 mx-auto">
                <p class="lead mb-4">Publish your unique Designs in front of thousands of your beloved customer for free.</p>
             <div class="d-grid gap-2 d-sm-flex justify-content-sm-center"></div>      
                <a href="/submit-design" class="btn btn-primary btn-dark btn-lg">Submit-Design</a>
            </div>
        </section>

<!--submit end-->

  